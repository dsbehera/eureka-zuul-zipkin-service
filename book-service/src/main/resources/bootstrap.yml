eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8081/eureka/
  #instance:
   # preferIpAddress: true
spring:
  application:
    name: BOOK-SERVICE
  sleuth:
    sampler:
      probability: 1.0